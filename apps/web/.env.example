# Set this to the deployed Cloudflare Worker URL for production builds.
# Leave it unset for local Vite development, which proxies /api/tables to :8787.
VITE_MULTIPLAYER_API_URL=https://whistzilla-multiplayer.YOUR-SUBDOMAIN.workers.dev
